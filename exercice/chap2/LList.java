
public class LList {
    Node head;

    public void add(int v) {
        Node n = new Node(v);
        if (head == null) {
            // it is the first node
            head = n;
        }
        while ( next != null) {
            // append n to tail
    }

}
